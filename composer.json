{
  "name": "roundcube-smtp-choice/smtp_choice",
  "type": "roundcube-plugin",
  "license": "MIT",
  "version": "1.1.0",
  "description": "Per-login SMTP in Roundcube using PHPMailer (same method as the standalone send tool).",
  "require": {
    "roundcube/plugin-installer": ">=0.1.3"
  }
}
